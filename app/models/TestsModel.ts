class TestsModel {
    id:number;
    title: string;
    subtitle: string;
    price: string;

    constructor(id:number, title: string, subtitle: string, price: string) {
        this.id = id;
        this.title = title;
        this.subtitle = subtitle;
        this.price = price;
    }
}